# Changelog

## Unreleased

- Phase 7 step 4.2 (referral lineage and field normalisation; partners D-7, D-10, and D-2, D-3, D-4, D-6, D-8 confirmed done earlier): a lead or deal is now linked to its partner through one helper (`getLeadPartnerId`, `findPartnerForLead`, `partnerFieldsFor`, `pricingLeadView` in `src/features/partners/partnerLink.js`) instead of a mix of `partnerId` and `agentId`. Choosing an agent on the lead card now writes `agentId`, `partnerId`, `partnerName`, `agentName` and the partner's rate (it wrote only `agentId`), and the quote uses the partner's current rate, so a partner with a rate no longer gets the LKR 30 default and the "defaulted" warning. Deals completion and the payment-cleared handler find the partner by either field, so a deal from the public referral form (which has `partnerId` and no useful `agentId`) now credits its partner (it was skipped). The public referral form no longer writes a made-up 53.5 rate (signed-out visitors cannot read partner records); the CRM resolves the partner's real rate. The Partners screen's public link now uses `<origin>/referral?ref=<id>` like the QR modal. The partner mobile dock already shows Profile, not Chat (D-8). 9 unit tests and 2 Deals component tests; one fails against the old Deals code. **Not in this step:** D-9 (partner-scoped reads, needs rules and the parked own-record work), D-11 and D-12 (step 4.3), D-1 (step 4.1), D-5 (held).
- Rollout facts (docs only): the owner confirmed the Firebase project is `print-to-frame-erp`, the app uses only the `(default)` Firestore database and the two `ai-studio-...` databases are unused; a read-only check found **no active Storage rules** in the live project, so partner document uploads, the public registration upload and fabrication blueprint uploads are expected to fail there (blueprints keep files under 500KB inline). Recorded in `docs/04_workflows/LIVE_ROLLOUT.md` and the `PLAN.md` backlog; the environment-check decision there is closed. The deployed Firestore rules text (copied from the console by the owner) was diffed against `main`'s `firestore.rules`: identical apart from trailing whitespace, so that file is the rollback for the rules steps. Also found: the original project folder `Print-To-Frame-ERP-System` is a separate repository with unrelated history and near-identical code, so it was unknown which repository the live Vercel project deploys from. The owner's screenshot and screen recording then confirmed it: production (`portal.print2frame.xyz`, Vercel project `print-to-frame-erp`) deploys `main` of the original repository, commit `f258270`, so merging pull request #25 here does not change the live site. The runbook step 2 now gives two paths: reconnect the live project to this repository (recommended) or bring the code into the original repository.
- Security fix, `api/send-email.js` (finding 1 of `docs/03_security/AUTHORIZATION_MAP.md`): the endpoint sends from the company mailbox and any approved account could use it with a free subject and body. It now (1) accepts only staff roles (Admin, Manager, Sales, Operations, Support, Accounts, Logistics; Partner, Business Client, Customer and unknown roles get 403), (2) accepts only the seven templates the app actually sends (`client_approval`, `client_activation_confirmed`, `partner_approval`, `partner_activation_confirmed`, `employee_invite`, `password_reset`, `registration_declined`) and returns 400 for free-form subject and body or any other template, and (3) rejects Deactivated and Disabled accounts even when `isApproved` is still true. Every caller in the app already sends one of those seven templates from a staff screen, so no flow changes. 23 API tests (was 9), seven of which fail against the old endpoint. The recipient is still not checked (backlog). This is server code: it takes effect on the next production deploy, not before.
- Security sweep (docs only): `docs/03_security/AUTHORIZATION_MAP.md` lists the seven places that grant, check or bypass access, explains that Firestore rules combine with OR (no rule or file overrides another; only a broad `allow` widens access), and records six findings: any approved account can send email from the company mailbox through `api/send-email.js`, the AI endpoint is open to any approved account, the dev proxy skips authentication and binds to `0.0.0.0`, the live rules leave `quotations`, `counters`, `messages`, `users` and `pricing` open to any signed-in account with no active-status check (fixed on `staging`, not deployed), public writes exist by design, and some risks (Firebase IAM, the service-account role) can only be checked in the consoles. No code changed.
- Live rollout preparation (docs only; nothing applied to the live project): new runbook `docs/04_workflows/LIVE_ROLLOUT.md` with facts read from the live project on 2026-09-21 (old rules still deployed, permission matrix unchanged since 2026-09-01 and lacking `quotations` and `receipts` for every role, `main` 108 commits behind `staging`), the ordered approval-gated steps (matrix migration first, then promotion, then additive rules, role check, restrictive rules last), pre-flight, verification and rollback for each. It corrects the earlier plan: the bare 3.4 rules file would reject the lead and deal id counters added in step 6.2, so the deployable additive version is branch `claude/rules-3-4d-deploy` (3.4 plus `L` and `D`, with a rules test that fails without it). The new code must not be promoted before the matrix is migrated, because `canAccess` denies a module a role does not have. Decisions settled the same day: no quiet window is needed for the matrix (additive, invisible to the old code), the migration is done with the Permissions Manager button on the staging preview (which uses the `(default)` database), and no role loses any access; a read-only check of the Vercel preview project found no database override and that `FIREBASE_SERVICE_ACCOUNT_JSON` there is not stored as Sensitive (backlog).
- Phase 7 step 8.2, tasks 11 and 12 (finish, no code changes): `docs/05_decisions/0003-source-layout.md` records the layout (`src/features/<domain>`, `src/shared`, `src/services`, the `@/` alias, the one dependency rule, what was deliberately not done and why); the README structure section and `PROJECT_INDEX.md` describe the final layout; the temporary codemod and markdown link checker (`scripts/refactor/`) are removed (they remain in git history from PRs #44 and #45). Four follow-ups were added to the `PLAN.md` backlog: splitting the files over 800 lines, a formatter, leftover repository hygiene, and the extra Firestore databases in `firebase.json`. 8.2 is complete.
- Phase 7 step 8.2, task 10 (admin into a feature folder, moves only, no behaviour change): `src/features/admin` (AdminPanel, AgentDatabase, PermissionsManager, adminUsers). 4 files moved with `git mv`; imports in 7 files and 9 docs rewritten by the codemod. `src/components/` no longer exists: `src/` is now `features/`, `shared/`, `services/`, `context/`, `constants/` plus `main.jsx`, `App.jsx` and `index.css`. `docs/01_architecture/SYSTEM_OVERVIEW.md` and root `CLAUDE.md` describe the final layout and the import rule. Verified with lint, the full test gate (the permissions and listener tests included), the build, the Playwright smoke test and the markdown link check.
- Phase 7 step 8.2, task 9 (operations into feature folders, moves only, no behaviour change): `src/features/fabrication` (FabricationWorks, FabricationCardDetails, FrameBlueprintPreview, cutListEngine, fabricationLink, qaGate) and `src/features/logistics` (Logistics, LogisticsCardDetails, logisticsEngine, logisticsTask). 10 files moved with `git mv`; imports in 17 files and 20 docs rewritten by the codemod; no shared, services, context, constants or `api/` file imports them. `googleMapsService` stays in `services/`. Root `CLAUDE.md` now points to `src/features/<domain>` and `src/shared` instead of the old component folders. Verified with lint, the full test gate, the build, the Playwright smoke test and the markdown link check.
- Phase 7 step 8.2, task 8 (money paths into feature folders, moves only, no behaviour change): `src/features/deals` (Deals, dealSettlement, dealProjectSync), `src/features/invoicing` (Invoices, Receipts, invoiceTemplate, receiptTemplate, invoiceSettlement, invoicePrintData) and `src/features/partners` (Partners, PartnerQRModal, and the two public pages PartnerRegistration and ReferralForm, which `src/main.jsx` still mounts without signing in). 13 files moved with `git mv`; imports in 18 files and 20 docs rewritten by the codemod; nothing in shared, services, context, constants or `api/` imports them. Verified with lint, unit, API, component and rules tests (the money-path component tests included), the build, the Playwright smoke test and the markdown link check.
- Phase 7 step 8.2, task 7 (CRM front half into feature folders, moves only, no behaviour change): `src/features/leads` (Leads, LeadCardDetails, leadLineage, audioProcessing), `src/features/customers` (Customers, ContactSyncModal) and `src/features/quotations` (QuotationBuilder, CostCalculator, pricingEngine, quotePricing, quotationStatus). 11 files moved with `git mv`; imports and 20 docs rewritten by the codemod; no shared, services, context or constants file imports any of them. `contactsService` stays in `services/` (it is a Google API client). Verified with lint, the full test gate, the build, the Playwright smoke test and the markdown link check.
- Phase 7 step 8.2, task 6 (first feature folders, moves only, no behaviour change): `src/features/auth` (Login, authFlow), `src/features/profile` (UserProfile), `src/features/dashboard` (Dashboard, NotificationsView) and `src/features/messaging` (Messages, MiniChatDrawer, FloatingMessageToast, MessagingContext, messageFilters). 10 files moved with `git mv`; 11 files' imports and 20 docs rewritten by the codemod. Nothing outside these features, `App.jsx` and the tests imported them, so no layering rule is bent (the dashboard imports the messaging feature, which is allowed). Verified with lint, 204 unit, 38 API, 57 component, 49 rules tests, the build, the Playwright smoke test and the markdown link check.
- Phase 7 step 8.2, task 5 (shared code, moves only, no behaviour change): the shared UI primitives moved from `src/components/common/ui/` to `src/shared/ui/`; the cross-feature components (`Card`, `DeleteModal`, `ErrorBoundary`, `LoadingSpinner`, `AddressPickerModal`, `GoogleDrivePickerModal`, `EmailTemplateModal`) to `src/shared/components/`; the domain-free helpers (`csvExport`, `dateUtils`, `entityUtils`, `events`, `stringMatch`, `toast`, `validation`) to `src/shared/utils/`. 34 files moved with `git mv` (history kept) and every import, `vi.mock` path and doc path rewritten by the codemod. `EmailTemplateModal` is no longer re-exported from the `shared/ui` barrel (three screens import it from `@/shared/components/EmailTemplateModal`), so `shared/ui` stays purely presentational. `googleMapsService` and `contactsService` stay in `src/services/` beside the other API clients (a small change from the plan), because `AddressPickerModal` in `shared` uses the Maps client and `shared` must not import a feature. Verified with lint, 204 unit, 38 API, 57 component, 49 rules tests, the build and the Playwright smoke test.
- Phase 7 step 8.2, task 4 (import alias, no file moves, no behaviour change): `@/` now means `src/` in Vite, Vitest (unit and component) and `jsconfig.json` for editors; every cross-folder relative import in `src/` and the tests (326 lines in 80 files, including the `vi.mock` and dynamic `import()` paths) was rewritten to it by a codemod, `scripts/refactor/move.mjs`, that will also do the file moves in tasks 5 to 10 (temporary, deleted in task 12). Two ESLint rules keep it that way: code in `src/` may not use `../` imports, and tests may not import from `src` by a relative path. `api/`, the root configs, Playwright and node fixtures keep relative paths because Vite does not process them. Verified with lint, 204 unit, 38 API, 57 component, 49 rules tests, the build and the Playwright smoke test.
- Phase 7 step 8.2, task 3 (docs layout, no code changes): each of the 16 modules is now one folder, `docs/02_modules/<module>/{README.md, CLAUDE.md, FINDINGS.md}` (the map used to sit beside the folder as `<module>.md`); every link and path mention was rewritten. Also repaired 75 links that were already broken (52 `file:///` links to a previous developer's local worktree in the notifications findings, 23 root-relative links in the post-merge report). `scripts/refactor/check-md-links.mjs` checks that every relative markdown link resolves; it is a temporary tool for the 8.2 refactor and is deleted in task 12.
- Phase 7 step 8.2, task 2 (remove unused files, no behaviour change): deleted `server.archive.js` (an old Express server that nothing runs or imports), `metadata.json` and `firebase-blueprint.json` (AI Studio leftovers), the empty `functions/` folder (there are no Cloud Functions), `scripts/` (two one-off docx generators no npm script called) and the 3MB test call recording in `public/`, each after a search found no reference; removed the unused dependencies `concurrently`, `glob` and `docx` (and the `vendor-docx` chunk entry for it in `vite.config.js`). Docs that mentioned `functions/` now say it does not exist. Still in `public/`: `portal-login-template.html` and `web and erp design theme.md`, which were not on the approved list.
- Phase 7 step 8.2, task 1 (contributor entry points, no code changes): the README is now about this project (setup, emulator mode, script table, structure, branches and deploys, docs) instead of the AI Studio template; new `.env.example` (variable names only, derived from the code), `.editorconfig` and `CONTRIBUTING.md`; `package.json` is named `print-to-frame-erp` (was `mcp-erp-app`), version 1.0.0, with `engines.node >= 22`. The plan for the rest of 8.2 (standard folder layout) is in `PLAN.md`.
- Phase 7 step 8.1 (docs sync, no `src` changes): `TESTING.md` coverage map refreshed from `npm run coverage` (242 unit and API tests, about 12.6% statements) with rows for every new util and component test, and the Phase 7 status added to its roadmap; root `CLAUDE.md` now lists all five test layers and says approving a Partner or Business Client hands off to the manual registration form; the cost-calculator, fabrication, deals and partners maps and instructions no longer describe the hidden discount, the fixed 53.5 commission, an unguarded duplicate Final invoice, or commission accruing at Hand Over (it accrues at Completed).
- Phase 7 steps 5.2 and 5.3 (auth; DP-04, DP-05, DP-07 and the Trigger 7a race): switching the sign-in screen between Login and Register no longer resets the role to `Customer` (a person registering after the toggle was requesting the wrong role) and no longer drops the company and specialty fields (`EMPTY_FORM`); registering no longer races the auth listener, which used to write a bare pending record and sign the user out before the full registration was saved (`registeringRef`, `newUserAction` in `src/features/auth/authFlow.js`), and the `REGISTER` audit entry is awaited before sign-out; an account that an administrator deactivates, disables or unapproves is signed out at once with a message instead of on its next login (`shouldEvict`; bootstrap admins are never evicted); every approved session start, Google sign-in and reload included, writes a `LOGIN` audit entry (the duplicate one in `handleLogin` is removed). Already done earlier: DP-01 (5.1), DP-02, DP-03, DP-06 (3.4, 3.5), DP-08 (the route guard typo is gone). Two tests are red without the fixes.
- Phase 7 step 6.5b (logistics D-6): the delivery card has a "Record cash collection" action (with a confirm step) for the job's unpaid invoice. It marks the invoice Paid through the same handler the Invoices screen uses (so the lead, full-settlement and commission effects still fire), stamps `paymentMethod: 'Cash (COD)'`, `collectedBy` and `paidAt`, then issues the receipt and writes a `COD_COLLECTED` audit entry (`handleCollectCod` in `src/App.jsx`, `getCollectableInvoice` in `src/features/logistics/logisticsEngine.js`). Nothing is collectable until the 25% Final invoice exists (the card says so), and it is hidden for a paid or cancelled invoice. **Only roles that can edit invoices and create receipts see it, which today is Admin and Manager: the Logistics role has read-only invoices and no receipts in the default matrix, so drivers cannot use it until you decide to widen that.** `handleMarkInvoicePaid` now returns success or failure. D-7 (fleet and driver directory in Firestore) is in the `PLAN.md` backlog because it needs a new settings document and rules.
- Phase 7 step 6.5a (logistics; D-3, D-5, D-8): Deals, Leads and Fabrication now create logistics tasks through one builder (`buildLogisticsTask`, `src/features/logistics/logisticsTask.js`) so every task carries `customerPhone`, `linkedJobNo`, `dealId`/`leadId`, priority and `createdAt` (Deals used to save the phone under `phone`, which the card never read, and pickups from Leads used a clock id, now `L-PK-0001` from a counter); moving a delivery task to In Transit or Completed writes `deliveryStatus` (`in_transit` / `delivered`) to its linked project, shown on the fabrication card, and moving it back clears it; a failed write on drag, forward, back or delete now puts the card back and shows "Failed to sync stage change to server". D-1, D-2 and D-4 were already done (5.x, 3.2, 2.3). Still open in logistics: D-6 (record cash collection in the field) and D-7 (fleet and driver directory in Firestore).
- Phase 7 step 6.4c (inspection): the fabrication card's QC sign-off milestone can no longer be ticked by hand and a save can never set `qaPassed` (only the QA Inspection Gate can, via `checklistWithGuardedQa`, `src/features/fabrication/qaGate.js`); the fabrication card no longer shows the contract value; the QA inspector is always the signed-in user (read-only field); each defect is kept in `defectHistory` (with an id) while `defectDetails` holds the active one and is cleared on a QA pass; `QA_PASSED`, `QA_DEFECT_FLAGGED` and `QA_REWORK_COMPLETED` are audit logged; a job is not marked Completed if its Final invoice fails to save (`handleSaveInvoice` now returns success or failure); the QA invoice picks up the customer phone and company from every alias on the job. Already done earlier: duplicate-Final guard and re-inspection (2.1), AI token (F-5), Operations invoice create permission (3.2). **Not done, in the `PLAN.md` backlog:** notifying the deal's sales owner on Revision (needs stored notifications) and the optional "Email client: QA passed" action.
- Phase 7 step 6.4b, part 2 (fabrication F-3, F-4, F-6; owner decisions 2026-09-21): the size measured on the lead (Length and Height in feet, saved with its pricing) now flows to the deal and to the fabrication job as frame width and height in millimetres and is read-only in the fabrication card, so it is never re-entered downstream (`dimensionsFromLead`, `src/features/fabrication/fabricationLink.js`; jobs from leads with no saved size stay editable). The manual job form has no price field: a manual job must be linked to a deal (it inherits the deal's customer and locked size and is billed through the deal) or explicitly marked non-billable internal / extra work, and is refused otherwise before a job number is used. Blueprint uploads go to Firebase Storage and keep only the URL, falling back to inline data under 500KB if Storage refuses and rejecting larger files. **Blueprint upload to Storage has not been tried against the live Storage rules.** Nothing else in Fabrication shows prices.
- Phase 7 step 6.4b (fabrication; F-8, F-9, F-11 and the F-3 sizing fix): the fabrication card no longer sizes a job with only a contract area off a frame 67% of that area (`defaultFrameDimensions` gives a 3:2 frame of the full area); the blueprint draws the real stiffener ribs from the cut-list (`vRibCount` / `hRibCount`, now returned by `calculateCutList`) instead of a fixed X-brace and states the tolerance as ±2.0mm (Structural Steel); the Admin check for deleting a job is case-insensitive and deletions write a `PROJECT_DELETED` audit entry; new projects (lead conversion and manual jobs) carry `customerId` (the customer's email, else NIC) so the customer-portal rule on `projects` can match. Already done earlier: F-1, F-2 (2.x), F-5 (AI proxy sends the token), F-7 (6.2), F-10 (6.6). **Not done here:** F-4 (manual-job Advance invoice, needs an owner decision), F-6 (blueprints to Firebase Storage, needs Storage rules), F-3's dimension capture on leads (leads have no frame size fields). All three are in the `PLAN.md` backlog.
- Phase 7 step 6.4a (deals; D-6, D-9, D-10 and the invoice reprint fix): moving a deal forward now moves its linked fabrication project forward too (Fabricating to Ongoing, Ready To Load to Ready For Inspection, Completed to Completed; matched by job number, never pulls a project back, `src/features/deals/dealProjectSync.js`); `Hand Over` deals appear in the dashboard action queue; deleting a deal writes a `DEAL_DELETED` audit entry, releases its original lead (`convertedToDeal: false`) and marks the project `Cancelled` with a reason, and never deletes invoices, receipts or logistics. Invoice reprint from the lead card now prints the saved invoice as issued (line items, customer name, company, phone) instead of mixing in the newest quotation and the lead's current details (`src/features/invoicing/invoicePrintData.js`); drafts are unchanged. The deals checklist D-1 to D-10 is complete (the rest were done in 2.x, 3.5 and 6.6). **A `Cancelled` project has no Fabrication column, so it leaves the board.** Still to come for 6.4: fabrication (6.4b) and inspection (6.4c).
- Phase 7 step 6.2 (atomic ids): lead ids (`L-0001`), deal ids (`D-0001`), job numbers (`PTF-0001`, from Leads convert and Fabrication), quotation ids (`QT-0001`) and delivery job ids (`L-DL-0001`, from Deals) now come from `generateAtomicId` (a Firestore transaction on `counters/<prefix>`) instead of the last digits of the clock, so two people acting in the same moment can no longer overwrite each other's record or link the wrong ids. If the counter cannot be reached the action stops with an error instead of falling back to a clock id. `firestore.rules` (NOT deployed) adds `L` and `D` to the known counter prefixes; the live rules let any signed-in user write counters, so this works today. **Not converted:** the public referral form (`LD-`) and partner registration (`APP-`) ids, because their users are not signed in and cannot write counters. Old ids (`L-123456`) stay as they are; a new counter could in theory reach one, which needs about 100,000 records.
- Phase 7 step 6.1 (leads, part 2 of 2): the Partners screen no longer lists a converted lead stub (its locked `Completed` stage made the commission look payable and it was counted alongside its Deal), so eligibility follows the Deal; new deals get a collision-checked `jobNo`; lead create, update and convert write audit log entries (`LEAD_CREATED`, `LEAD_UPDATED`, `LEAD_CONVERTED`); the dead Convert button in the Completed column is removed. A Deal now counts as payable only when its invoices are fully paid (or `referralStatus` is set), no longer on stage alone. Invoices are matched through the lead lineage (`getLineageIds` / `invoicesForLineage` in `src/features/leads/leadLineage.js`: the record's own id, `originalLeadId` and `convertedDealId`), because an invoice can be keyed by either the original lead id or the Deal id. Three new component tests and two unit tests.
- Phase 7 step 6.1 (leads, part 1 of 2): "Convert to Deal" in the lead card only shows for leads in the `Received` stage; saving a quotation sets the lead's `value` to the quote's grand total; saving a lead no longer creates a customer or bumps `orders` (customers are created only when the lead is converted); oversized compressed call recordings (MP3, M4A, AAC, OGG, WEBM over 3.2MB) are downsampled like WAV instead of being sent raw and risking a 413. Not in this PR: partner commission gating (Action B), lead audit logging, `jobNo` collisions, the dead Completed-column Convert button. No component test covers the stage gate (the lead card is not yet under test).
- Phase 7 step 5.1 (Google scopes): sign-in keeps the identity scopes only. Drive and Contacts access is requested on demand through `getScopedAccessToken(scope)` in `src/services/firebase.js` (a Google popup for that one scope, hinted with the signed-in email), cached per scope in `sessionStorage` for 55 minutes because Google tokens last an hour and Firebase does not refresh them, and cleared on sign-out. `driveService` (`drive.readonly`) and `contactsService` (`contacts.readonly`) use it, so both no longer fail with 403. The Drive and Contacts screens open a Google consent popup the first time they are used, so they must be started from a click.
- Phase 7 step 6.3 (customers): new `normalizePhone` and `phonesMatch` in `src/shared/utils/validation.js` (spaced, `07` and `+94` spellings of one number are equal, empty never matches). Lead-to-customer matching in `Leads.jsx` (save and convert), the customer stats lookup, and the Google Contacts import in `Customers.jsx` now compare normalised phones, so spaced numbers no longer create duplicate customers. The contact import also no longer skips every phone-only contact because `"" === ""`. One B2 characterisation test flipped.
- Phase 7 step 6.6 (pricing): `calculateCost(tier, sqFt, discountPct = 0, commissionRate = 0)` has no hidden 15% discount and no fixed 53.5 commission, and Profit / SQ is gross profit per sq ft. The Cost Calculator gets Discount % and Partner commission inputs; the lead card uses the partner's rate and 15% only for referral leads. Quotations: a new version becomes the active quote immediately, attached Drive files are saved and restored, and generating the Advance invoice marks the quote Invoiced (treated as Accepted everywhere via `isAcceptedQuote`). Stock-bar orders use first-fit-decreasing packing instead of a 5% linear allowance. Three B1 characterisation tests flipped. **Quotes for direct leads now come out higher than before (no 15% discount) but exclude the old 53.5 commission.** A referral lead whose partner has no commission rate is quoted at LKR 30.00 per sq ft (`getQuotePricingTerms` in `src/features/quotations/quotePricing.js`), the lead's `pricingMetadata.commissionRateDefaulted` is set, and a warning notification asks an Admin or Manager to update the partner's rate.
- B2 supporting unit tests: `validation`, `stringMatch` and `csvExport`, plus one phone-matching characterisation test recorded in the `TESTING.md` register. No `src/` changes.
- Recorded the work deferred until the live site is ready to change (`docs/05_decisions/0002-deferred-until-live-rollout.md`, `PLAN.md`): live matrix migration and rules deploys, the Partner own-record restriction, B6 journeys, partners D-5, server-side counters, separate environments.
- CI trimmed: the workflow no longer runs on pushes to `staging`, and a `changes` job skips `lint-unit`, `rules` and `e2e` for pull requests that only touch `docs/` or `*.md`.
- Phase 7 step 3.5 (`firestore.rules`, NOT deployed): permission checks require an approved, active account (Deactivated/Disabled always denied; a legacy approved document without status keeps working); quotations use the quotations permission; messages are readable only by participants and Admin and can only be sent as yourself into your own conversations; users are readable by Admin, self, or roles with agents or messages view; leads are readable via leads or pipeline and written by isDeal; delete follows each module's delete permission; Managers with the agents permission administer non-Admin users (never Admins, never their own role) and review pending sign-ups. `DEFAULT_PERMISSIONS` gives Manager full `agents`. The users listener now lists the collection only for roles that may, and the pending listener also opens for roles that can edit users. **Deploy order: 3.3 (matrix adds quotations and receipts) then 3.4d then this.**
- Phase 7 step 3.4 (`firestore.rules`, NOT deployed): `isAdmin()` accepts the bootstrap emails; pending applicants can update (not approve) their own record; customers can update their own profile fields; new `referral_claims` and `partner_payouts` blocks; counters limited to known prefixes and at most one step ahead. Partners D-5 (public read of Active partners) held because it would expose bank details. Six B4 characterisation tests flipped, one new gap recorded (counters can still be lowered).
- Phase 7 step 3.3 (prepared, no live write): Permissions Manager shows a banner and an "Add N missing modules with defaults" button when the saved matrix lacks modules (today `receipts` and `quotations`); it fills only absent cells from `DEFAULT_PERMISSIONS`, leaves the rest untouched, and nothing is saved until an Admin reviews and clicks Save.
- Phase 7 step 3.6 (`api/admin-user.js`): a Deactivated or Disabled caller is rejected even with `isApproved: true`; Managers may now call the endpoint but cannot grant Admin or act on an Admin's account; the token email is trimmed and lowercased before lookups. Two B3 characterisation tests flipped.
- Phase 7 step 3.2 (client RBAC): App opens each Firestore listener only when the role can read it (quotations follow leads/pipeline until every live matrix has a `quotations` module; partner applications are Admin only); `DEFAULT_PERMISSIONS` gains a `quotations` module and changes Manager receipts delete to false, Manager `admin` to none, Operations invoices to view+create, Logistics invoices to read, Partner partners to view+edit, Customer and Business Client receipts and messages to none; PermissionsManager adds the Quotations row and locks the System Overview module to Admin. `PERMISSIONS_FIXTURE` synced. **The live `settings/permissions` document is unchanged; it needs step 3.3.**
- Coverage map refreshed after Phase 7 step 2 (about 9% of `src` and `api` statements from unit and API tests); roadmap in `TESTING.md` now lists B2 and B6 as the open items.
- Phase 7 step 2.6 (invoice policy): invoice id and type are immutable and only Admin or Manager can change the amount; an invoice with a receipt cannot be deleted and unpaid invoices can be Cancelled instead; the dead `lead.invoiceGenerated` / `invoiceDate` reads are gone (the convert modal derives the Advance invoice from `invoices`); paying a single full-settlement invoice now flips `invoicePaid` (new `invoiceSettlement` helper), on the lead and on the deal it converted into.
- Phase 7 step 2.5 (receipts): `handleGenerateReceipt` refuses an invoice that is not Paid; the receipt amount is read-only (the invoice amount) in Invoices and the lead card, with an optional Notes / Reference field that is forwarded; Receipts disables Export CSV when empty and catches export errors; the delete audit entry falls back to the user's identifier; invoices created from Deals, QuotationBuilder and FabricationWorks carry `partnerId`. The App-level unpaid-invoice guard has no test (not reachable from the UI).
- Phase 7 step 2.4: new `src/features/deals/dealSettlement.js` (`getFinalInvoiceAmounts`, `calculateDealCommission`). Deal completion bills 25% of the highest-version Accepted quotation (or stored balanceDue, else the deal value), saves that total as `deal.value`, prices the fallback line at the full value, and estimates commission from value (÷850) when the deal has no square footage without adding area. `invoiceTemplate` applies each line's discount and tax before the 25%/75% scaling; the x0.25 print scaling is kept by decision.
- Phase 7 step 2.3: `calculateCODFromInvoices` counts only the latest unpaid Final invoice and returns `finalInvoicePending` with the 25% shortfall for a paid Advance with no Final; Logistics card, details panel and waybill show "Pending 25% Settlement Invoice Creation" instead of "all settled". Two characterisation tests flipped.
- Phase 7 step 2.2: Deals and Fabrication no longer offer a backward move out of Completed; bulk stage change rejects Completed; deal completion records `commissionAccrued` and never accrues partner commission twice. No test for the bulk-change rejection (table selection UI not exercised).
- Phase 7 step 2.1: `getExistingFinalInvoice` in `entityUtils`; deal completion and job QA pass no longer create a second Final invoice (FabricationWorks now receives `invoices`), and the quotation "25% Final Settlement" button shows only for a Deal or after the Advance is paid. The guard is client-side, so a race between two sessions is still possible. Two B5 characterisation tests flipped.
- Phase 7 step 1 (quick wins): sign-out clears notifications; mobile dock uses the `projects` module and Partners get a Profile button instead of Messages; the WhatsApp update and route-insight AI calls go through `generateText` (bearer token attached); registration email is trimmed and lowercased; the notification feed drops the user's own messages; removed the dead `onUnreadCountChange` prop and the false "encrypted" copy in Messages.
- B5 component wiring tests: `Deals`, `FabricationWorks`, `Partners` and an `App` sign-out flow rendered with `firestoreSync`, toast and audit log mocked; four characterisation tests (duplicate Final invoice from deal completion and QA pass, phantom payout, sign-out notification leak). No `src/` changes.
- B4 rules tests: `tests/integration/rulesAccess.test.js` (permission-gated collections, settings, audit log, public forms), 13 characterisation tests for known rule gaps and 12 `it.todo` targets for Phase 7 3.4 and 3.5. No `firestore.rules` change.
- B3 API handler tests: `admin-user` caller checks (two characterisation tests), `generate` gate and model fallback, `send-email` gate and payload; Firebase, Gemini and SMTP mocked. No `api/` changes.
- B1 money-path unit tests: `pricingEngine`, `invoiceTemplate`, `receiptTemplate`, `firestoreSync` pure helpers and extra COD cases in `logisticsEngine`; six characterisation tests recorded in the `TESTING.md` register. No `src/` or `api/` changes.
- `PLAN.md` now records the Phase 7 workflow (branch, local test, `staging`, then `main`), the owner decisions and the step dependencies; T0 (suite on `main`) is ticked.
- Coverage map refresh is a manual, tracked task in `PLAN.md` (not required per change).
- `TESTING.md` is now the planning reference: a "Planning a change" checklist, a coverage map, a characterisation register, the Part B roadmap and known gotchas. Root `CLAUDE.md` tells planning sessions to read it.
- Added `.nvmrc` (Node 22) and switched the CI workflow to `node-version-file: .nvmrc`; no `engines` field, so the Vercel runtime is unchanged. `jsdom` is back on `^29` now that Node 22 is the baseline.
- E2E runner (A6): `@playwright/test`, `playwright.config.js` (starts the emulators and `dev:emulated`), `tests/e2e/global-setup.js` (fails the run unless emulators are local and the project id starts with `demo-`, then seeds), `npm run test:e2e`, and a sign-in smoke journey.
- Emulator wiring (A5): guarded `connectFirestoreEmulator`/`connectAuthEmulator`/`connectStorageEmulator` block in `src/services/firebase.js` (flag plus dev server or `demo-` project), committed `.env.test`, `npm run dev:emulated`, idempotent `tests/fixtures/seed.mjs` with `npm run seed:emulator`.
- CI (A7): `.github/workflows/test.yml` with `lint-unit` and `rules` jobs for pull requests to `staging`/`main`; README "Running the tests" section.
- Rules test harness (A4): `tests/helpers/emulator.js` (`setupRulesEnv`, `seedPermissions`, `asRole`, `clearAll`); `firestoreRules` and `invoiceNumbering` integration tests now use it with unchanged assertions.
- Component test layer (A3): `vitest.component.config.js` (jsdom), `tests/helpers/setupComponent.js`, `renderWithProviders`, `npm run test:component`, RTL devDependencies, and a StatusBadge smoke test.
- API test layer (A2): `tests/helpers/mockHttp.js`, `npm run test:api`, `tests/api/README.md`, and a gate smoke test for `api/admin-user.js`; `test:all` and `coverage` now include the API tests.
- Testing foundations (A1): `docs/04_workflows/TESTING.md`, ESLint now lints `tests/`, `coverage` script with v8 reporters (no thresholds), `tests/helpers/factories.js` and a factories smoke test, removed the dead `src/utils/e2eTestSuite.js`, added the testing/remediation tracks to `PLAN.md`.
- Adopted the erp-system folder structure: `docs/` (architecture, modules, security, workflows, decisions), `functions/`, `.claude/`, `PROJECT_INDEX.md`.
- Added `.gitignore`.
- Recovered files missing from the org repo (`src/App.jsx`, `src/index.css`, `src/services/`, `src/utils/`, `crm/{Invoices,Partners,Receipts}.jsx`, `README.md`) from the original project folder.
- Investigation Phases 1-5: filled `GCP_INVENTORY`, `SYSTEM_OVERVIEW`, 16 module maps, `CROSS_MODULE_TRIGGERS`, `RBAC_MODEL`, `FIRESTORE_RULES_NOTES`, `GIT_WORKFLOW`, `DEPLOY_PROCESS`, and per-module `CLAUDE.md` files. Root `CLAUDE.md` gained a layout / module-index section.
